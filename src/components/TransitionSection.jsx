import React from 'react';
import '../styleCompants/TransitionSection.css';

const TransitionSection = () => {
  return (
    <section className="transition-section">
      {/* Blank section for future content */}
    </section>
  );
};

export default TransitionSection;
